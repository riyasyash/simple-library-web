<div class="container">
  <% @books.each do |books| %>
      <div class="gridContent">
        <div class="tip">
          <img class="bookimage" src="<%=books.thumbnail%>" alt="Thumbnail" width=100px></img>
          <h4 class="card-title"><%=books.title%></h4>
          <span><p><%=books.description%></p></span>
        </div>
        <%if books.available > 0%>
          <%= link_to "Borrow", checkout_book_path(:isbn => books.isbn),method: :post, :class => 'myButton'%>
        <%else%>
          <%= link_to "Request", request_book_path(:isbn => books.isbn),:class => 'myButton'%>
        <%end%>
      </div>
  <%end%>
</div>
